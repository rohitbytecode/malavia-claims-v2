import type { TimelineEvent } from "../../types/domain";
import { formatDateTime, labelize, nameOf } from "../../utils/format";
import { StatusBadge } from "../ui/StatusBadge";
export function ClaimTimeline({ events }: { events: TimelineEvent[] }) { const sorted = [...events].sort((a, b) => new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime()); return <div className="timeline">{sorted.map((event, index) => <article className="timeline-item" key={event.id ?? event._id ?? `${event.type}-${index}`}><div className="timeline-icon">{event.type[0]}</div><div className="timeline-content"><div className="timeline-head"><strong>{event.title ?? labelize(event.type)}</strong><time>{formatDateTime(event.createdAt)}</time></div><div className="timeline-meta">{event.toStatus && <StatusBadge value={event.toStatus} compact />}{event.severity && <StatusBadge value={event.severity} compact />}<span>{nameOf(event.actor)}</span></div>{event.fromStatus && event.toStatus && <p>{labelize(event.fromStatus)} → {labelize(event.toStatus)}</p>}<p>{event.remarks ?? event.message ?? "No remarks recorded"}</p>{event.attachmentName && <span className="secure-file">Secure attachment: {event.attachmentName}</span>}</div></article>)}</div>; }
